## Packages
recharts | Data visualization for impact reports
framer-motion | Smooth animations for page transitions and UI elements
date-fns | Date formatting for impact entries

## Notes
Authentication uses Replit Auth (via useAuth hook).
API routes follow /api/programs, /api/impact patterns.
Impact data requires dynamic form fields based on program metrics.
